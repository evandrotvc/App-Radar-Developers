import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';


ReactDOM.render(<App />, document.getElementById('root'));

/* Sempre importa react em todo arquivo JS, quando for usar HTML, tag HTML: <App />
tag na qual chamada no react JSX = JAVASCRIPT + XML(sintaxe do html)

*/

